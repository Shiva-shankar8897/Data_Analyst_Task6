# Data Analyst Task6
### Using Sql Server (SSMS)
In this we have to extract a year,month from a column, group by the segregate the column, sorting the order using order by query,
aggregrating the sum, count the no.of values and top query is used to limit the data from a column.
